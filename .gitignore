# Git 버전 관리에서 제외할 파일 목록을 지정하는 파일
# 예를 들어 올리고 싶지 않은 파일명 등을 이곳에 적으면 올라가지 않는다.

# Mac-OS 관련
.DS-Store

# Directories
/Note/*

# 현재 Repository의 cache를 모두 삭제한다.
# $ git rm -r --cached .

# [File Name]에 해당하는 파일을 원격 저장소에서 삭제한다.
# (로컬 저장소에 있는 파일은 삭제하지 않는다.)
# $ git rm -r --cached [File Name]

# 출처: https://gmlwjd9405.github.io/2017/10/06/make-gitignore-file.html